

# Want to use this class to initiate objects that can be added to a list and identified 
class part:

    def __init__(self, contour, color, isIdentified):
        self.contour = contour 
        self.color = color
        self.isIdentified = isIdentified

    
